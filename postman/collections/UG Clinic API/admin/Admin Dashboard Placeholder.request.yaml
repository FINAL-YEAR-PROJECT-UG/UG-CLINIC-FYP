$kind: http-request
name: 'Admin Dashboard Placeholder'
method: GET
url: '{{baseUrl}}/api/admin/dashboard'
order: 1000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Admin dashboard returns 501 (not yet implemented) or 200', function () {
        pm.expect(pm.response.code).to.be.oneOf([200, 401, 403, 501, 500]);
      });
