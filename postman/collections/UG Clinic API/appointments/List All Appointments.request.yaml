$kind: http-request
name: 'List All Appointments'
method: GET
url: '{{baseUrl}}/api/appointments/all'
order: 2000
headers:
  - key: Authorization
    value: 'Bearer {{accessToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('List all appointments returns expected outcome', function () {
        pm.expect(pm.response.code).to.be.oneOf([200, 401, 403, 500]);
      });
